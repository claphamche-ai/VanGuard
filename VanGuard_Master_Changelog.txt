VANGUARD MASTER CHANGELOG
-------------------------
v4.8 - Accounts UI Refinement (Current Build)
- Cleared background inheritance (.admin-bg) from accounts.html to stop nested iframe box effects.
- Added dynamic click-to-edit row logic in the Accounts list.
- Expanded the User Schema to include `name`, `contact`, and `status` (Active/Inactive).
- Wired `status` flag into the login router (`index.html`) to physically block disabled accounts.
- Added a License Enforcement counter. The system calculates Active users against the Tenant's maximum allowed license limit, restricting the creation or activation of users if the limit is hit.
